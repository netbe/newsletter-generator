%h1 Articles
.links= link_to 'New article', new_article_path
%ul
- articles.each do |article|
  %li
    = link_to article.title, article_path(article), :class=> "thickbox"
    = link_to '[edit]', edit_article_path(article)
